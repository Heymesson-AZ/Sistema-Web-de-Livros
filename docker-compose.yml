services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel_app
    restart: always
    ports:
      - "80:80"
    volumes:
      - .:/var/www
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
